{
  "mcpServers": {
    "pubchem-mcp-server": {
      "command": "npx",
      "args": ["pubchem-mcp-server"],
      "env": {
        "MCP_LOG_LEVEL": "debug",
        "MCP_TRANSPORT_TYPE": "http",
        "MCP_HTTP_PORT": "3010"
      }
    }
  }
}
